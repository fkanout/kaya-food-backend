export const DB_COLLECTIONS = {
    ORDERS: 'orders',
    USERS: "users"
} as const
export const VERIFICATION_STATUS = {
    PENDING: "pending",
    VERIFIED: "verified",
    UNKNOWN: "unknown"
} as const
