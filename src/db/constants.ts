export const DB_COLLECTIONS = {
    SMS_AUTH: "sms_auth",
    USERS: "users"
} as const
export const VERIFICATION_STATUS = {
    PENDING: "pending",
    VERIFIED: "verified",
    UNKNOWN: "unknown"
} as const
